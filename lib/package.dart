class Package {
  String id, name, address, price, quantity, area, type, packagetype, startdate, starttime, endtime, description, nightstay, buffet, coffee, latitude, longitude, 
  url, contact, imagename, date;
  Package({
    this.id,
    this.name,
    this.address,
    this.price,
    this.quantity,
    this.area,
    this.type,
    this.packagetype,
    this.startdate,
    this.starttime,
    this.endtime,
    this.description,
    this.nightstay,
    this.buffet,
    this.coffee,
    this.latitude,
    this.longitude,
    this.url,
    this.contact,
    this.imagename,
    //this.booked,
    this.date
  });
}